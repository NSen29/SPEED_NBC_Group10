import React from "react";

const Home = () => {
    return ( <
        div >
        <
        h2 className = 'sm:text-4xl text-2xl font-bold mt-6 text-gray-900' > WELCOME TO SPEED < /h2> <
        p > I am not sure what to put on the home page < /p> <
        p > A nice logo ? Instructions ? < /p> < /
        div >
    );
}

export default Home;